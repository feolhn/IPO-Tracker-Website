{
  "name": "ipo-tracker-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "data:update": "node scripts/update-data.mjs",
    "data:full": "IPO_FULL_REFRESH=1 node scripts/update-data.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "dependencies": {
    "echarts": "^6.1.0"
  }
}
